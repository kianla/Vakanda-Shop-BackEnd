export class Items {
    constructor(public id: number, public name: string , public discription: string, public photo: string, public item_count: Number ,public price: Number,public group_id: Number ) {}
  }
